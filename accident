<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Subscribe to Newsletter</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Sansita:wght@400;700;800&family=Anybody:wght@300;400;500&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: #1e3a34;
            color: #f2e7da;
            font-family: 'Anybody', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .subscribe-container {
            max-width: 600px;
            width: 100%;
            text-align: center;
        }

        h1 {
            font-family: 'Sansita', sans-serif;
            font-size: 3.5rem;
            margin-bottom: 1rem;
            color: #f2e7da;
        }

        .subtitle {
            font-family: 'Anybody', sans-serif;
            font-size: 1.1rem;
            line-height: 1.6;
            margin-bottom: 2rem;
            color: #f2e7da;
            font-weight: 300;
        }

        .form-container {
            background: rgba(255, 255, 255, 0.05);
            padding: 2rem;
            border-radius: 12px;
            backdrop-filter: blur(10px);
        }

        .input-group {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        input[type="email"] {
            padding: 1rem;
            font-size: 1rem;
            border: 2px solid rgba(242, 231, 218, 0.3);
            border-radius: 8px;
            background: rgba(255, 255, 255, 0.1);
            color: #f2e7da;
            font-family: 'Anybody', sans-serif;
            transition: all 0.3s ease;
        }

        input[type="email"]::placeholder {
            color: rgba(242, 231, 218, 0.6);
        }

        input[type="email"]:focus {
            outline: none;
            border-color: #ff6f61;
            background: rgba(255, 255, 255, 0.15);
        }

        button {
            width: 100%;
            padding: 1rem 2rem;
            font-size: 1.1rem;
            font-weight: 500;
            background-color: #ff6f61;
            color: #f2e7da;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-family: 'Anybody', sans-serif;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        button:hover {
            background-color: #ff5a4d;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(255, 111, 97, 0.3);
        }

        button:active {
            transform: translateY(0);
        }

        button:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

        .message {
            margin-top: 1rem;
            padding: 1rem;
            border-radius: 8px;
            font-family: 'Anybody', sans-serif;
            display: none;
        }

        .message.success {
            background: rgba(76, 175, 80, 0.2);
            border: 1px solid #4caf50;
            color: #4caf50;
            display: block;
        }

        .message.error {
            background: rgba(244, 67, 54, 0.2);
            border: 1px solid #f44336;
            color: #f44336;
            display: block;
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2.5rem;
            }

            .subtitle {
                font-size: 1rem;
            }

            .form-container {
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="subscribe-container">
        <h1>Subscribe</h1>
        <p class="subtitle">Subscribe to our newsletter to be the first to know about upcoming events, activations, and all things happening in Denver and surrounding areas!</p>
        
        <div class="form-container">
            <form id="subscribeForm">
                <div class="input-group">
                    <input 
                        type="email" 
                        id="email" 
                        name="email" 
                        placeholder="Enter your email address" 
                        required
                    >
                </div>
                <button type="submit" id="submitBtn">Subscribe</button>
                <div id="message" class="message"></div>
            </form>
        </div>
    </div>

    <script>
        const form = document.getElementById('subscribeForm');
        const emailInput = document.getElementById('email');
        const submitBtn = document.getElementById('submitBtn');
        const messageDiv = document.getElementById('message');

        form.addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const email = emailInput.value.trim();
            
            if (!email) {
                showMessage('Please enter a valid email address', 'error');
                return;
            }

            submitBtn.disabled = true;
            submitBtn.textContent = 'Subscribing...';
            messageDiv.style.display = 'none';

            try {
                const response = await fetch('https://api.beehiiv.com/v2/publications/d1c3a035-75ee-419d-b4a1-d5eb8d9cbbf7/subscriptions', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({
                        email: email,
                        reactivate_existing: false,
                        send_welcome_email: true,
                        utm_source: 'website',
                        utm_medium: 'organic'
                    })
                });

                if (response.ok) {
                    showMessage('Successfully subscribed! Check your email to confirm.', 'success');
                    emailInput.value = '';
                } else {
                    const errorData = await response.json();
                    showMessage(errorData.message || 'Subscription failed. Please try again.', 'error');
                }
            } catch (error) {
                showMessage('An error occurred. Please try again later.', 'error');
            } finally {
                submitBtn.disabled = false;
                submitBtn.textContent = 'Subscribe';
            }
        });

        function showMessage(text, type) {
            messageDiv.textContent = text;
            messageDiv.className = `message ${type}`;
        }
    </script>
</body>
</html>
